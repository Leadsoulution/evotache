// Bump whenever public/app-logo-light.png or app-logo-dark.png changes —
// plain files under /public keep the same URL forever, so browsers cache
// them aggressively; changing the query string is what forces a refetch.
export const LOGO_VERSION = "2";
